{
  "name": "image-loading-strategies",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && npm run sentry:sourcemaps",
    "preview": "astro preview",
    "astro": "astro",
    "sentry:sourcemaps": "sentry-cli sourcemaps inject --org the-claw --project image-loading-strategies ./dist && sentry-cli sourcemaps upload --org the-claw --project image-loading-strategies ./dist"
  },
  "dependencies": {
    "@sentry/browser": "^7.69.0",
    "@sentry/cli": "^2.20.7",
    "astro": "^3.1.0"
  }
}